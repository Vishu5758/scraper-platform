# LLM Status

Use this document to record operational status for LLM-dependent features.

## Snapshot
- LLM normalization available via pipeline pack agent; testing recommended before production use.
- No other LLM-based agents enabled by default.

Update with latency, cost, and quality observations as experiments run.
